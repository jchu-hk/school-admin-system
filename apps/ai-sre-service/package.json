{
  "name": "@school-admin/ai-sre-service",
  "version": "0.1.0",
  "description": "AI SRE — 通用可部署、可学习、可支持新系统的旁路运维 Agent 服务（Issue #370）",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test:intake": "ts-node test/intake.smoke.ts",
    "test:query": "ts-node test/query.spec.ts",
    "start": "node dist/main.js",
    "dev": "ts-node src/main.ts"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
