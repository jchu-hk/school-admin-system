# 2026-08-18 (Tue) — Daily Memory

# 09:00 — Heartbeat (Tue) 🟢 第165轮 零变化 (#309备份E2E第十四晚有效 | 磁盘89%稳定 | 全绿)

### System Status 🟢 (内网正常) / 🟢 (磁盘89% 稳定) / 🟢 (全绿)
- 与 21:04 第164轮一致,零实质变化。backend 8081 `/api/health` **200 (0.009s)**, frontend 8080 200, gateway 9000 401(需token,在线)。服务健康。✅
- **#309 备份E2E 持续第十四晚有效**: `backup_20260817180000..sql.gz` **101808 bytes 非空** (8/18 02:00 生成), `gzip -t` VALID。连续第十四个非空夜间 dump(#309 修复持续生效)。 #309 保持 CLOSED ✅。
- **磁盘 89% (4.5G free)** 稳定,无回弹。✅
- host up ~2d1h, load 0.56 正常。Docker 全 Up (13 containers; postgres/redis/opa/kafka healthy), 仅 cloudflared Exited(废弃 #310, 已知)。✅
- **GitHub**: 49 open, **0 bug-labeled**, 0 P0/P1 bug (P0/P1 均 enhancement 特性 backlog)。#309+#310 均 CLOSED。零播报。

# 13:04 — Heartbeat (Tue) 🟢 第166轮 零变化 (#309备份E2E持续有效 | 磁盘89%稳定 | 全绿)

### System Status 🟢 (内网正常) / 🟢 (磁盘89% 稳定) / 🟢 (全绿)
- 与 09:00 第165轮一致,零实质变化。backend 8081 `/api/health` **200 (0.0025s)**, frontend 8080 200, gateway 9000 401(需token,在线)。服务健康。✅
- **#309 备份E2E 持续有效**: `/var/backups/school_admin/backup_20260817180000..sql.gz` **101808 bytes 非空** (8/18 02:00 生成), `gzip -t` VALID。连续第十四个非空夜间 dump(#309 修复持续生效)。 #309 保持 CLOSED ✅。
- **磁盘 89% (4.5G free)** 稳定,无回弹。✅
- host up ~2d5h, load 0.41 正常。Docker 全 Up (13 containers; postgres/redis/opa/kafka healthy), 仅 cloudflared Exited(废弃 #310, 已知)。✅
- **GitHub**: 49 open, **0 bug-labeled**, 0 P0/P1 bug (P0/P1 均 enhancement 特性 backlog)。#309+#310 均 CLOSED。零播报。

## [DEVOPS] T25 (Issue #261) 部署脚本/DB迁移/env — commit c937e59
- 新增迁移 1782530900000-CreateQrAttendanceAndPortalLeaveTables（qr_codes/attendance_qr_logs/offline_sync_buffer/attendance_daily_reports/leave_requests）
- infra/deploy/ 新增 migrate-db.sh / deploy-backend.sh / deploy.sh / ENV-VARS.md
- 识别生产缺失 env: QR_SIGNING_MASTER_KEY / FRONTEND_URL / WEBHOOK_SECRET；DB_SSL 未同步
- 迁移在 scratch DB 验证幂等；Docker Hub 受限，采用 docker cp + docker exec 无重建部署

## [DEVOPS] T26 (Issue #262) UAT 环境就绪 — 完成
- 产出 `docs/UAT-ACCESS-SHEET.md`（含凭据，标注勿外泄）；已 comment #262 + 发送完成消息。
- 环境可达性实测：Coze proxy /school-admin/ /portal/student /portal/parent /attendance/qr /school-admin/api/health 全 200。
- UAT 账号就绪（登录实测）：school_director=`director`、teacher=`teacher_1a`（两者登录后需邮箱OTP，码在后端日志 `docker logs school-admin-backend`）；student=`student1`、parent=`parent1`（免OTP直登）。
- 关键发现1: director/teacher/system_admin **代码级强制邮箱OTP**（auth.service.ts requiresOtpVerification；与 otp_enabled 无关）。本环境 NOTIFICATION_CHANNEL=mock 不真发信，OTP 打后端日志。
- 关键发现2（交QA/DEV）: 部署的 portal leave entity `@Entity('leave_requests')` 指向 DB **不存在的表**（现只有 `leaves`）。HEAD 迁移 CreateQrAttendanceAndPortalLeaveTables 会建 leave_requests+offline_sync_buffer（IF NOT EXISTS 幂等）但**未部署** → 电子请假功能可用性需复核。
- 版本核对（**曾误判后更正**）: backend 实际=HEAD(`53e2c31`)，非落后。`53e2c31`仅改 Dockerfile/debs/部署脚本未动 backup.service.ts；容器 Dockerfile与HEAD逐字一致、pg_dump16.15在位、备份14+晚非空。前端 admin v1.6.1/2c7680c 与台账相符。
- 环境限制: Docker Hub/registry 不可达（docker.io、registry-1 均timeout）；无独立公网tunnel（cloudflared停，走Coze proxy）。
- 健康基线: 13容器全Up无unhealthy；磁盘40G/89%used(余4.5G)；内存3.8Gi/3.3Gi used/无swap(avail~579Mi)。
- 附: PROJECT-WIKI 已更新测试环境部署台账 JS hash 与 backend 版本状态。

## [QA-Round3] 外部 UAT UI 终验（Issue #262 / 浏览器冒烟 / 2026-08-18）
- 用 agent-browser 访问外部 Coze proxy base URL，逐页截图+校验 DOM 挂载+`/assets/*` HTTP 状态。
- **portal 白屏已消除** ✅：`/assets/index-BTSIfyoS-20260707.js|css`（portal 构建）→ 200。家长门户(/portal/parent,张小玲脱敏档案)、扫码页(/attendance/scan,李老师/启动扫码) 完整渲染 → PASS。QR 页(/attendance/qr) shell+date+QR占位渲染但数据接口 Unauthorized；学生门户(/portal/student) 渲染 Unauthorized 错误页。
- **`/school-admin/` 仍白屏** ❌：admin 为**独立构建**，index.html 引用根 `/assets/index-B52kQjGo-20260707.*`；实测 `/school-admin/assets/...`→200 根 `/assets/...`→404。DEVOPS 的 nginx `location /assets/→:8081` 只 serve portal 构建，漏 admin 构建 → admin 页 JS 404 白屏。需 DEVOPS 补 admin 构建根路径资源。
- **新发现**：`fetch("portal/menus")` 经 proxy 返回 200 但 body 是 SPA index.html（非 JSON）→ proxy 未路由前端 API 到后端 → portal/student 与 QR 报 Unauthorized（独立于 /assets 白屏）。
- 结论：外部 UAT 前端 ⚠️ **未完全 ready**（portal 侧放行，admin 后台仍白屏）。截图 `/tmp/qa_shots_final/`；评论 #262 `issuecomment-5325996846`；完成消息已发 PM（type=done,status=idle）。
