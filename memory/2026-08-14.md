# 2026-08-14 (Fri)

## 07:25 — 用户授权 #309 备份修复部署 + 1 周后复盘

用户消息（webchat）: "Let proceed. Take a post-enable review after 1 week comparing the result before & after."

理解: 授权部署 #309 备份修复（已 commit `b5ae579`），并约定 1 周后（2026-08-21）复盘，对比「启用前 vs 启用后」的备份结果。

### 部署前基线 (BEFORE) — 供 1 周后复盘对比（关键）
- 当前 backend 容器（`school-admin-backend`，image `6f3ac69d...`）**缺 pg_dump**：`which pg_dump` → `NO pg_dump`
- bind-mount `/var/backups/school_admin` **已挂载**（EACCES 已解，属 nestjs 可写）
- 但每日备份文件**全部 20 字节空文件**（`backup_20260806...~20260812...sql.gz` 均 20B）→ 伪成功（缺 pg_dump + 旧 backup.service.ts 无非空校验）
- 历史: #309 自 2026-07-14 起累计 72 次失败（EACCES），bind-mount 解 EACCES 后仍因缺 pg_dump → 空备份
- 文件名有 `..sql.gz` 双点异常（备份命名疑似小 bug，非本次重点）

### 修复内容（commit `b5ae579`，已提交未部署）
1. Dockerfile 装 `postgresql-client`（pg_dump，之前缺失 → 空备份）
2. `backup.service.ts` pipefail + 非空校验（防伪成功）
3. compose bind-mount + `BACKUP_DIR` env（已生效）
4. `run-backend.sh` 可复现启动脚本

### 动作
- spawn DEVOPS 部署: `cd infra && docker compose build backend && docker compose up -d backend`
- 部署后验证: pg_dump 可用 + 触发测试备份确认非空
- cron: 2026-08-21 08:00 复盘（对比 before/after）

### 复盘指标 (AFTER vs BEFORE)
- 备份文件大小: 20B(空) → 应 >几十KB(真实 dump)
- pg_dump: 缺失 → 存在
- backup_records: 失败/空 → 成功

---

## 07:40 — DEVOPS 部署受阻：docker daemon 无 bridge 网络

DEVOPS 尝试 `docker compose build backend` → 失败。**非代码 bug，是环境约束**。

根因: `/etc/docker/daemon.json` 配置 `"bridge": "none"` + `"iptables": false` → build 容器无网络 → `RUN apt-get install postgresql-client` 无法下载 → build 失败。运行栈 100% 未变。

补充事实（已核实）:
- 宿主机**有网络**（curl debian 镜像 200）
- `school-admin-postgres` 容器**自带 pg_dump**（/usr/local/bin/pg_dump）
- 宿主机无 pg_dump；backend 容器无 pg_dump（空备份根因）
- 新代码（pipefail+非空校验）在 main（b5ae579），但运行容器是旧 image 旧 dist

### 可选路径（待用户决策）
1. ✅ 推荐：离线内嵌 .deb 重建（宿主机下 .deb → Dockerfile `COPY`+`dpkg -i` 替代 apt-get → 重建）。永久、不改 daemon、仅 backend 重启。需 Dockerfile 小改 → spawn DEV。
2. 快速止血（非永久）：`docker cp` pg_dump 进运行中 backend → 今晚备份即可生效，但容器重建即丢。
3. 破坏性：改 daemon `bridge`+`iptables` + 重启 dockerd → 恢复正常 build，但会重启所有容器（需明确授权）。

状态: 待用户选择路径。

---

## 08:10 — #309 已解决 ✅（Option #1 离线内嵌 + pg_dump 升级到 16）

用户选 Option #1（离线内嵌 .deb + 重建）。执行链：DEV → DEVOPS →（版本不匹配）→ DEV 升级 → DEVOPS 终验。

### 两次关键阻塞（都已解决）
1. **daemon bridge=none** → `docker compose build` 默认网络失败。解法：`docker build --network=host`（宿主机有网络，离线 dpkg 装 .deb，build 全程无需网络）。
2. **pg_dump 版本不匹配**：Debian bookworm 主仓库只有 postgresql-15（15.18），但 DB 是 `postgres:16-alpine`（16.14），pg_dump 15 拒绝 dump 16。解法：从 pgdg bookworm-pgdg 仓库取 postgresql-client-16（16.15）+ libpq5 18.6 + client-common 293，dpkg 离线安装。

### 最终状态
- 镜像 `school-admin-backend:v1.5.9`（内置 pg_dump 16.15），容器 `8cdc5582711d`
- 备份实测产出 **101,531 字节**真实 SQL（旧版 20 字节空文件）→ 对比鲜明
- pipefail + 空文件守卫均编译生效（pipefail 2 处 + `size===0` throw 1 处）
- health 200；仅动 backend 容器，未改 daemon、未碰其他容器

### 已提交
- commit `53e2c31`（`fix(backup): #309 offline-embed pg_dump 16.15 via dpkg`），已 push main（27e27ab..53e2c31）
- 改动：`apps/backend/Dockerfile`（apt-get→COPY+dpkg）+ `apps/backend/debs/`（3 个 .deb，共 ~2.28MB）

### 遗留事项
1. **待 1 周复盘**（cron 已设 2026-08-21 08:00）：对比 before（20B 空文件/无 pg_dump/72 失败）vs after（101KB 真实 dump/pg_dump 16.15）。
2. **次要技术债**：运行中 `school-admin-backend` 容器**不属于 `infra` compose 项目**（compose 镜像名 `infra-backend`），部署须用直接 `docker run` 而非 compose；建议后续统一（另行开 issue，非本次范围）。
3. 备份文件名双点 `..sql.gz` 异常未处理（非本次重点）。

## 08:11 — Heartbeat: #309 已部署✅ + 磁盘 98% 告急

- **#309 部署成功**: DEVOPS 采用离线内嵌 pg_dump 路径(commit `53e2c31 fix(backup): #309 offline-embed pg_dump 16.15 via dpkg`),替代 apt-get,未改 daemon。新 backend 容器(Start 08:11, image e824acb4) pg_dump 16.15 可用,health 200。
- **备份效果待今晚验证**: 最近备份仍为 02:00 的 20B 空文件(部署前)。下轮 18:00 应产出非空 dump → 验证后 close #309。
- **⚠️ 磁盘 98% 满 (804M free)**: 自 81% 基线骤降。Docker 可回收 ~9.2G(build cache 3.5G + dangling images 5.7G + volumes 0.3G)。建议 DEVOPS/主机 `docker builder prune` + 清理 dangling images 释放空间。
- #310 公网 🔴 持续(cloudflared Exited, 需 host egress,非 agent 可推)。

## 09:00 — Heartbeat: #309 已部署实测验证✅ / 磁盘98%告急 / #310持续

- **#309 已解决并实测验证 ✅**: 部署后容器(image e824acb4) pg_dump 16.15 可用,health 200。内存确认手动实测备份产出 101,531B 真实 SQL(旧 20B 空文件),pipefail+空守卫生效。待今晚 18:00 定时备份端到端确认后 close #309。
- **⚠️ 磁盘 98% 满 (817M free)**: 持续告急未缓解。Docker 可回收 ~9.2G(build cache 3.5G + dangling images 5.7G + volumes 0.3G)。当前唯一活跃可推进项。建议 DEVOPS/主机 `docker builder prune` + 清理 dangling images。
- **#310 公网 🔴 持续**(cloudflared Exited, 需 host egress, 非 agent 可推)。
- System: load 0.60, backend health 200, frontend Up 9h。
